# Logs
logs
*.log*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.tmp
*.tsbuildinfo
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Claude Code worktrees and local settings
.claude/worktrees/
.claude/settings.local.json
# Local-only orchestration / coordination notes (multi-agent session details
# that don't belong in the production repo)
.claude/*.md

# Local-only review folder used during landing-page comparison
landing-previews/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# wrangler files
.wrangler
.dev.vars*
.prod.vars
.env
.env.*

runner/downloads
/generated/prisma

/generated/prisma
test.ts
temp/

templates
debugging.ipynb

#bun.lock
#*.lock

debug-tools/*.json
debug-tools/extracted
debug-tools/presentation-tester
# Sentry Config File
.env.sentry-build-plugin
data-dump

# Upstream-sync additions (Phase E PR 2a)
.test-data
cli

# Development artifacts (root directory only)
/*.PNG
/*.png
screencapture-*.png
SANDBOX_*.md
deploy*.log
# Cross-session-comms: local pointer to current M3 lead Claude Code session
.m3-lead-session

# M3 port scratch files (upstream reference, never commit)
.upstream-*.ts

# Local operator-seed credentials (never commit plaintext)
.seed.json
*.seed.json
.cont-spike.env
